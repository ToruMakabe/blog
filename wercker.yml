box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
        theme: purehugo
        flags: --buildDrafts=true	
deploy:
  steps:
    - leipert/git-push:
        gh_oauth: $GIT_TOKEN
        gh_pages_domain: torumakabe.github.io
        basedir: public
        repo: ToruMakabe/torumakabe.github.io