box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
        theme: purehugo
        flags: --buildDrafts=true	

deploy:
  steps:
    - git-push:
         gh_oauth: $GIT_TOKEN
         gh_pages: true
         gh_pages_domain: torumakabe.github.io
         basedir: public